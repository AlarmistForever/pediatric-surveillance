[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pediatric-surveillance-mvp"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
pediatric-surveillance = "pediatric_surveillance.cli:main"

[tool.setuptools]
packages = ["pediatric_surveillance"]
